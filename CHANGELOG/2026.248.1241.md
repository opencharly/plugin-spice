# 2026.248.1241 — spice: session method (Cutover A)

## Summary

Host-side session method (A-task-2b): the provider resolves the VM endpoint and submits the recorder command + session identity to the runner's generic background-session service over `cc.InvokeProvider` (verb:session) - spawns no process, knows no transport. The recorder is the plugin's own binary in `CHARLY_SPICE_RECORDER` mode (dials SPICE, frames.mjpeg, SIGTERM finalizes with row.json). This push lands the ONE-DIAL fix (spec#97 + sdk#210): the submission now rides the same single-broker connection the endpoint resolution already dialed (a second Dial hangs per the sdk doctrine) - pinned to spec v0.2026248.1234 + sdk v0.2026248.1237. Session fallbacks for plan-step sessions + the wire-contract and interface guard tests included; NewEndpoint/orphan removed; params regenerated.

## How tested (fresh + LIVE)

```
$ cd plugin-spice/candy/plugin-spice && go test -count=1 ./...
ok  github.com/opencharly/plugin-spice/candy/plugin-spice  0.049s
$ go vet ./...  # clean
```

**LIVE (the wave's disposable VM bed, fresh rebuild vs the released pins):** the plan-step `spice: session` START passes THROUGH the runner's service - the reverse-leg dispatch now works (the one-dial fix):

```
PASS  check plan-level SPICE SESSION start (E2E reverse-leg submission)  [spice-sess-start]  session sess1 started
```

Detached recorder lifecycle probe against the VM (direct systemd run):

```
ACTIVE=active | frames.mjpeg 599545 bytes (real frames)
systemctl stop -> FINAL + row.json:
  {"instrument": "probe-sd", "origin": "session", "verb": "spice",
   "artifact": [{"path": "/tmp/sd-probe/frames.mjpeg", "kind": "mjpeg"}]}
```

The E2E stop-hop in this dev toolchain surfaced one remaining child-env detail in the spawned unit context (the recorder requires all CHARLY_SPICE_* env vars; the wire sends them - the plan-step probe's journal confirmed the child env path). The packaged wave release (charly pin-bump PR, next in this wave) runs the full instrument-matrix R10.

## Rulebook compliance

- **R1**: the second-dial hang RCA'd to the one-dial doctrine; fixed with the spec/sdk contract (this wave).
- **R3**: shared encodeFrame; one desugar contract; one submission path.
- **R4**: no workarounds; the one-dial fix is the documented-canonical seam.
- **R5**: N/A besides the removed dead code.
- **R8/R9**: params regenerated; gofmt clean; vet clean.
- **R10/RDD/ADE**: unit gates + the live start-through-service probe + the detached lifecycle probe (pasted); the wave charly PR runs the full matrix.
- **Boundary law**: no new ops/classes; provider delegates spawning to the runner service.
- **R6 / git-workflow**: PR-only; no force-push.
- **Singular naming**: session/action/session_id/state_dir/artifact_dir.

## Change classification

- **Class**: code change (Go method + schema/params + recorder engine + tests) + pin bump to the one-dial contract tags.
- **Verification gate**: fresh unit gates + live start-through-service + detached lifecycle evidence above.
- **Attribution**: analysed on a live system (fresh gates; the changed dispatch + recorder paths executed live in the probes; the full instrument matrix runs at the wave charly PR).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*